<form method="post" action="{{ path('product_has_specification_delete', {'id': product_has_specification.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ product_has_specification.id) }}">
    <button class="btn">Delete</button>
</form>
