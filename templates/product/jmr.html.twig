{% extends 'base.html.twig' %}

{% block title %}Product index{% endblock %}

{% block body %}
<h1>Cart index</h1>
{#    <table class="table">#}
{#        <thead>#}
{#        <tr>#}
{#            <th>Id</th>#}
{#            <th>Prodname</th>#}
{#            <th>Description</th>#}
{#            <th>Img</th>#}
{#            <th>actions</th>#}
{#            <th>img</th>#}
{#            <th>Categorie</th>#}
{#            <th>Spec type</th>#}
{#            <th>price</th>#}
{#        </tr>#}
{#        </thead>#}
{#        <tbody>#}
        {% for product in products %}
{#            <tr>#}
{#                <td>{{ product.id }}</td>#}
{#                <td>{{ product.prodname }}</td>#}
{#                <td>{{ product.description }}</td>#}
{#                <td>{{ product.img }}</td>#}
{#                <td>#}
{#                    <a href="{{ path('product_show', {'id': product.id}) }}">show</a>#}
{#                    <a href="{{ path('product_edit', {'id': product.id}) }}">edit</a>#}
{#                </td>#}
{#                <td><img src="{{ product.img }}" alt="je moer"></td>#}
{#                <td>{{ product.categorie }}</td>#}
<td>{% for specification in product.specifications %}

        {#                        {{ specification }}#}
    {% endfor %}
    {#                </td>#}
    {#                <td>{{ product.price }}</td>#}
    {#            </tr>#}
    {% else %}
        {#            <tr>#}
        {#                <td colspan="5">no records found</td>#}
        {#            </tr>#}
    {% endfor %}
    {#        </tbody>#}
    {#    </table>#}
    <div class="jmr">
        <div class="pech">
            {% for product in products %}
                <div class="card">
                    <img src="{{ product.img }}" alt="Denim Jeans" style="width:100%">
                    <h1>{{ product.prodname }}</h1>
                    <p class="price">€{{ product.price }}</p>
                    <p>{{ product.description }}</p>
                    <p>{% for specification in product.specifications %}

                            {{ specification }}
                        {% endfor %}</p>
                    <button value="minus" id="{{ product.id }}" class=" drgn btn-primary">-</button>
                    <button value="plus"  id="{{ product.id }}" class=" drgn btn-primary">+</button>
                    <br>
                    <p>Amount:{{ product.amount }}</p>
                </div>
            {% endfor %}
        </div>
    </div>

    {#<div>#}
    {#    <button class="btn-primary">-</button>#}
    {#    <button class="btn-primary">+</button>#}
    {#</div>#}


    <style>
        .card {
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            max-width: 300px;
            margin: auto;
            text-align: center;
            font-family: arial;
        }

        .price {
            color: grey;
            font-size: 22px;
        }

        .card button {
            border: none;
            outline: 0;
            padding: 12px;
            color: white;
            background-color: #000;
            text-align: center;
            cursor: pointer;
            width: 100%;
            font-size: 18px;
        }

        .card button:hover {
            opacity: 0.7;
        }

        .jmr {
            display: flex;
            justify-content: space-between;
        }

        .pech {
            display: flex;
            justify-content: space-evenly;
            flex-wrap: wrap;
        }

    </style>
    <script>
        let arrbutton = document.getElementsByClassName('drgn');
        for (i = 0; i < arrbutton.length; i++) {
            arrbutton[i].addEventListener('click', function (event) {
                ajax(event.target)
            })
        }

        function ajax(element) {
            fetch('localhost:8000/drum/', {
                method: 'POST',
                body: JSON.stringify({
                    "email": "hello@user.com"
                })
            }).then(function (response) {
                return response.json()
            }).then(function (data) {
                // Do a backflip or something else cool
            })
        }

    </script>
    <a href="{{ path('product_new') }}">Create new</a>
    {% endblock %}
